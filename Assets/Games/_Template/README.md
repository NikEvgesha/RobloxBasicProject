# Game Template

Copy this folder to create a new game:

```text
Assets/Games/{GameName}
```

Keep game-specific scenes, prefabs, scripts, UI, configs, art, and audio inside the copied game folder.

If a script or prefab becomes useful to multiple games, move it into `Assets/GameKit` and document it.
